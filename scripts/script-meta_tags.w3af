# This is the test for grep.meta_tags

plugins
grep meta_tags

output console,text_file
output config text_file
set fileName output-w3af.txt
set verbose True
back

discovery web_spider
discovery config web_spider
set onlyForward True
back

back

target
set target http://moth/w3af/grep/meta_tags/index.html
back

start

assert len(kb.kb.getData('meta_tags','meta_tags')) == 2 

exit
