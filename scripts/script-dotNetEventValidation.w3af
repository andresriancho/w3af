# grep demo for dotNet Event Validation

plugins

output console,text_file
output

output config text_file
set fileName output-w3af.txt
set verbose True
back

output config console
set verbose False
back

grep dot_net_event_validation

back

target
set target http://moth/w3af/grep/event_validation/event_validation.html
back

start

assert len(kb.kb.getData('dot_net_event_validation','dot_net_event_validation')) == 2

exit
