# This is the htaccessMethods demo
plugins
audit htaccessMethods
grep httpAuthDetect
output console
output config console
set verbosity 0
back
back
target
set target http://localhost/w3af/basicAuthBrute/
back
start

assert len( kb.kb.getData('httpAuthDetect', 'auth') ) == 2
assert len( kb.kb.getData('htaccessMethods', 'auth') ) == 1

exit
