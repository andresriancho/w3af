# This is the LDAP injection demo

plugins
audit LDAPi
output console
output config console
set verbose False
back
back
target
set target http://localhost/w3af/LDAP/ldap.php?i=f00
back
start

assert len( kb.kb.getData('LDAPi', 'LDAPi') ) == 1

exit
