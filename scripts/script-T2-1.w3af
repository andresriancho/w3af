# This script is the first demo I showed at www.t2.fi

plugins
bruteforce formAuthBrute
discovery allowedMethods, webSpider, error404page, fingerPKS
grep passwordProfiling

output console,textFile
output
output config textFile
set fileName output-w3af.txt
set verbosity 10
back
output config console
set verbosity 0
back
back

target
set target http://localhost/w3af/formLogin/formLogin.html
back

start

assert len(kb.kb.getData('formAuthBrute','auth')) == 1

exit
