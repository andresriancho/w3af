# This script shows how oracle_discovery works

plugins
output console,text_file
output config text_file
set fileName output-w3af.txt
set verbose True
back
output config console
set verbose False
back

discovery oracle_discovery
back

target
set target http://moth/w3af/
back

start

assert len(kb.kb.getData('oracle_discovery','info')) == 1

exit
