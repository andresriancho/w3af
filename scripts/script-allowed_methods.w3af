# check allowed_methods

plugins
output console,text_file
output config text_file
set fileName output-w3af.txt
set verbose True
back
output config console
set verbose False
back
discovery allowed_methods
back

target
set target http://moth/w3af/
back

start

assert len( kb.kb.getData('allowed_methods', 'methods') ) == 1
assert len( kb.kb.getData('allowed_methods', 'dav-methods') ) == 0

exit
