# grep demo that find HTTP dump in response body

plugins
output console
output config text_file
set fileName output-w3af.txt
set verbose True
back
output config console
back

discovery web_spider
discovery config web_spider
set onlyForward True
back
grep objects
back

target
set target http://moth/w3af/grep/objects/index.html
back

start

assert len( kb.kb.getData( 'objects', 'object' ) ) == 1
assert len( kb.kb.getData( 'objects', 'applet' ) ) == 1

exit
